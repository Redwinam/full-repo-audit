interface:
  display_name: "Full Repository Audit"
  short_description: "Audit full repositories with resumable coverage ledgers"
  default_prompt: "Use $full-repo-audit to audit this entire project in report-only mode and report in my preferred language."
